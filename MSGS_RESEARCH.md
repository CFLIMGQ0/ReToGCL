# 多空间门控相似度模块（MSGS）研究记录

## 研究问题

现有图分类表示可能同时存在两类几何错误：同类图在分类空间相距很远，或异类图在分类边界附近相距很近。MSGS 尝试从语义、拓扑和节点分布三个空间估计图间关系，并使用样本对级门控根据分类不确定性动态融合相似度。

## 文献查重结论

截至 2026 年 8 月，检索到的工作已经分别覆盖了本想法的多个组成部分：

- Multi-view Adaptive Graph Convolutions（ECCV 2020）使用多个可学习距离和视图池化完成图分类。
- Similarity-aware Positive Instance Sampling（NeurIPS 2021）使用领域相似度从训练图中选择正样本。
- CoCo（ICML 2023）结合 GNN 与图核分支进行显式/隐式拓扑多视图对比。
- Smoothed GCL（LoG 2024）根据图中邻近关系平滑正负样本惩罚。
- Fuzzy Boundary GCL（ICML 2025）直接研究局部凝聚、全局稀疏和模糊边界。
- WLGCL（2026 年 TMLR 在审）结合特征相似度和 WL 结构相似度，并使用多正样本与困难负样本。

因此，“多视图”“结构与语义相似度”“困难样本”或“边界”单独都不能作为创新点。当前未发现与以下组合完全同构的工作：

1. 面向多图分类而非单图节点任务；
2. 同时使用图级语义、显式拓扑统计和节点表示分布；
3. 对每个图样本对生成三路门控，而不是全局固定权重；
4. 将分类预测熵作为门控条件；
5. 用融合相似度筛选和约束困难正负样本。

这只是工程开始前的检索结论，不等价于正式的新颖性证明。投稿前仍需进行更系统的数据库检索和逐篇方法对照。

## 已实现内容

### 三空间诊断

运行：

```bash
python src/scripts/diagnose_multispace_similarity.py --device cpu
```

诊断严格在每个外层折内完成：

- 线性分类器只使用训练折；
- 拓扑特征标准化参数只由训练折估计；
- 节点分布原型只用训练折节点拟合；
- 测试折标签只用于计算 Pair AUC、Recall@K 和边界 Recall@K。

输出为 `outputs/multispace/diagnostics.json`。

### MSGS 消融模型

运行 MUTAG 与 PROTEINS 预实验：

```bash
python src/scripts/run_multispace_ablation.py \
  --datasets MUTAG PROTEINS \
  --device cpu
```

九种消融配置：

| 配置 | 含义 |
|---|---|
| `baseline` | GraphCL-GIN 初始化，仅分类损失 |
| `semantic` | 只使用语义相似度 |
| `topology` | 只使用拓扑相似度 |
| `distribution` | 只使用节点分布相似度 |
| `uniform` | 三空间等权平均 |
| `global` | 三个全局可学习权重 |
| `gate` | 样本对动态门控，不使用边界信息 |
| `boundary_gate` | 加入分类预测熵的动态门控 |
| `full` | 边界门控加困难正负样本挖掘 |

所有配置使用相同分层五折、折内验证集和 GraphCL 初始化参数，最终报告 Accuracy、NMI、ARI 和 Macro-F1。门控版本在推理时只检索当前外层折的训练图，使用分类熵控制相似度投票对原始预测的修正强度；测试图之间不建立关系，也不使用测试标签。

## 当前诊断发现

- MUTAG：拓扑 Pair AUC 明显高于语义空间，均匀融合进一步提高 Pair AUC。
- PROTEINS：拓扑空间比语义空间提供更好的 Pair AUC，说明存在互补结构信号。
- NCI1：单空间 Pair AUC 接近随机，但三空间融合提高 Recall@5。
- COLLAB：语义空间当前最强，简单均匀融合没有提高 Pair AUC。
- COLORS-3：简单拓扑统计无效，节点分布与语义高度相关；需要更强的 WL、谱或 motif 表示。

这说明门控有研究价值，但“三个现有分支全部有效”的假设不成立。下一步应以预实验消融结果决定是否替换节点分布或拓扑分支，而不是直接全量运行。

## 首轮预实验结果

训练期关系正则版本在 PROTEINS 上由匹配微调基线的 72.95% 提升到 74.12%，但仍低于原始 GraphCL 线性探针的 75.29%。这说明只在训练阶段约束关系不足以直接修正边界预测。

加入“测试图查询训练折样本库”的归纳式检索后：

| 数据集 | 匹配微调基线 | 最佳 MSGS | 原始 GraphCL 线性探针 |
|---|---:|---:|---:|
| MUTAG | 70.73 ± 8.60 | 83.56 ± 13.04（`full`） | 83.54 ± 7.23 |
| PROTEINS | 72.95 ± 2.04 | 75.38 ± 1.87（`global`） | 75.29 ± 2.73 |

检索能够显著修复监督微调造成的性能下降，但目前只与原始 GraphCL 准确率持平，Macro-F1 仍略低。PROTEINS 上全局权重优于样本对门控，边界条件又略弱于普通门控；困难样本版本在 PROTEINS 上下降到 73.58%。因此当前结果尚不能证明复杂门控或边界信号优于简单全局融合，不应直接扩展成全数据集正式实验。

下一版优先事项：

1. 将高度相关的节点分布分支替换为 WL 子树或谱热核距离；
2. 在冻结 GraphCL 表示上训练门控，避免监督微调破坏已有表示；
3. 单独校准边界不确定性，而不是直接使用未校准 softmax 熵；
4. 先要求门控在 PROTEINS 上稳定超过 GraphCL 的 Accuracy 和 Macro-F1，再扩展到五数据集。
