# 数据集说明

## 数据概览

项目当前包含五个 TU Dortmund 图分类数据集：NCI1、PROTEINS、COLLAB、MUTAG 和 COLORS-3。每个数据集包含多个带图级标签的图样本。

## 数据来源

五个数据集均通过 PyTorch Geometric 的 `TUDataset` 接口从 TU Dortmund 图核数据集仓库下载。本项目使用原始版本，即 `cleaned=False`。COLORS-3 额外设置 `use_node_attr=True`，保留其 5 维节点属性。

## 目录结构

```text
datasets/
└── main_data/
    ├── NCI1/
    ├── PROTEINS/
    ├── COLLAB/
    ├── MUTAG/
    └── COLORS-3/
```

每个数据集目录下的 `raw/` 保存原始文件，`processed/data.pt` 是 PyTorch Geometric 生成的可直接加载文件。

## 标签定义

- TU 数据集用于图分类。每个 `Data` 对象表示一个图，`Data.y` 保存该图的类别编号。这些数据集不提供统一的训练、验证和测试划分，实验时需要单独定义可复现的划分或交叉验证策略。

## 数据统计

TU 图分类数据集的实际加载统计如下。边数采用 PyG 的双向边存储计数：

| 数据集   | 图数量 | 节点总数 |     边总数 | 平均节点 |   平均边 | 节点特征 | 边特征 | 类别数 |
| -------- | -----: | -------: | ---------: | -------: | -------: | -------: | -----: | -----: |
| NCI1     |  4,110 |  122,747 |    265,506 |    29.87 |    64.60 |       37 |      0 |      2 |
| PROTEINS |  1,113 |   43,471 |    162,088 |    39.06 |   145.63 |        3 |      0 |      2 |
| COLLAB   |  5,000 |  372,474 | 24,572,158 |    74.49 | 4,914.43 |        0 |      0 |      3 |
| MUTAG    |    188 |    3,371 |      7,442 |    17.93 |    39.59 |        7 |      4 |      2 |
| COLORS-3 | 10,500 |  643,773 |  1,911,576 |    61.31 |   182.05 |        5 |      0 |     11 |

COLLAB 原始数据不包含节点特征。模型若要求节点输入特征，应在实验方案中明确记录所采用的结构特征构造方式，例如常量特征或节点度编码。

COLORS-3 的图标签表示绿色节点数量。原始节点属性共有 5 维，其中第 2–4 列为颜色二进制编码，第 1 列为未归一化注意力值；实验加载时必须启用节点属性，不能用常量特征替代。

## 数据清洗

待补充去重、缺失值处理、异常样本过滤、格式转换和质量检查规则。所有会改变数据内容的操作应保留可复现脚本，并避免直接覆盖不可恢复的原始数据。

## 数据划分

- TU 数据集本身不提供固定划分。正式实验前应确定随机种子、分层划分或交叉验证策略，并保证各模型使用完全相同的样本划分。

## 相关脚本

下载全部 TU 图分类数据集：

```bash
python3 src/scripts/download_tu_datasets.py
```

仅下载指定的 TU 数据集：

```bash
python3 src/scripts/download_tu_datasets.py --datasets NCI1 MUTAG COLORS-3
```

## 注意事项

- 默认从 `datasets/main_data/` 组织数据读取路径。
- 不将数据文件放入 `src/`。
- 提交版本控制前，应检查数据许可、隐私风险和文件体积。
- 数据结构、清洗规则或划分发生变化时，应同步更新本文档。
